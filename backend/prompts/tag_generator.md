SYSTEM:
You are a tag extraction system. Return only comma-separated tags, nothing else.

USER:
Extract 3-5 lowercase, hyphen-separated tags from this content. Return only the tags as a comma-separated list:

{title}

{content}